1. imports redux { createStore, compose, replaceReducer ...} где в доках
2. imports react { component ...} где в доках
3. методы react (componentDidMount, set-get-State, ...) где в доках? Какие есть виды? Такие как render, это же не совсем методы, это обязательная функция, какие есть еще такие фунции или свойства?
4. redux методы к store ( subscribe, dispatch... ) где их искать и что они значат?

5. Что означает этот worning?
  ./src/lessons/lesson2 - toggle isShow/Cell.js
  Line 23:  img elements must have an alt prop, either with meaningful text, or an empty string for decorative images  jsx-a11y/alt-text

6. 