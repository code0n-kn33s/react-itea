import React, { Component } from 'react'

export default class TheoryReact extends Component {
  render() {
    return (
      <>
        <h1>React</h1>
        <ul>
          <li>1. imports react component ...} какие элементы можно доставать помимо component</li>
          <li>2. методы react (componentDidMount, set-get-State, ...) Какие есть еще виды?</li>
          <li>3. Такие как render, это же не совсем методы, это обязательная функция, какие есть еще такие фунции или свойства?</li>
        </ul>
      </>
    )
  }
}