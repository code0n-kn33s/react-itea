import React, {Component} from 'react'

export default class TheoryRedux extends Component {
  render () {
    return (
      <>
        <h1>Redux</h1>
        <ul>
          <li>1. redux методы к store ( subscribe, dispatch... ) где их искать и что они значат?</li>
          <li>imports redux --> createStore, compose, replaceReducer ...} какие еще виды ?</li>
        </ul>
      </>
    )
  }
}